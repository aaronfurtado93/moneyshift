import { SessionStorageItems } from './session-storage-items';

describe('SessionStorageItems', () => {
  it('should create an instance', () => {
    expect(new SessionStorageItems()).toBeTruthy();
  });
});
